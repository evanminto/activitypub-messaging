{
  "@context": "https://www.w3.org/ns/activitystreams",
  "type": "Person",
  "id": "<%= user_url(@user.username) %>",
  "name": "<%= @user.name %>",
  "preferredUsername": "<%= @user.username %>",
  "inbox": null,
  "outbox": null
}
